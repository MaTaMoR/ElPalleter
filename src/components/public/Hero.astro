---
export const prerender = true;

import lightBanner from '../../assets/banner-light.svg'; // Siempre usar este (blanco)
import Text from '../i18n/Text.astro';
import HeroBackground from './HeroBackground.astro';
---

<HeroBackground 
    imageId="hero-main"
    alt="Fondo principal El Palleter"
    className="hero-main animate-in"
>
    <!-- Header: Logo siempre blanco -->
    <div class="hero-header">
        <div class="hero-logo-container">
            <!-- Solo logo blanco - sin cambio de tema -->
            <img 
                src={lightBanner.src} 
                alt="El Palleter Logo" 
                class="hero-logo hero-logo-fixed"
            />
        </div>
    </div>
    
    <!-- Footer: CTA y scroll indicator siempre blancos -->
    <div class="hero-footer">
        <a href="#carta" class="hero-cta" data-scroll-to="carta">
            <Text key="hero.menu" />
        </a>
        <div class="scroll-indicator" data-scroll-to="historia">
            <div class="scroll-arrow">↓</div>
        </div>
    </div>
</HeroBackground>

<style>
    /* === LAYOUT HERO USANDO DESIGN TOKENS === */
    
    .hero-header {
        width: 100%;
        text-align: center;
        flex: 1;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        padding-top: calc(var(--padding-mobile) * 5);
        padding-left: var(--padding-mobile);
        padding-right: var(--padding-mobile);
    }

    /* === CONTENEDOR DE LOGO === */
    .hero-logo-container {
        position: relative;
        max-width: 650px;
        width: 100%;
        height: auto;
        margin-bottom: var(--space-8);
        aspect-ratio: 16 / 9;
        display: flex;
        align-items: center;
        justify-content: center;
        contain: layout style;
        overflow: hidden;
    }

    /* === LOGO FIJO EN BLANCO - SIN CAMBIO DE TEMA === */
    .hero-logo-fixed {
        width: 100%;
        height: 100%;
        object-fit: contain;
        object-position: center;
        
        /* Sombra siempre oscura para contraste con logo blanco */
        filter: drop-shadow(0 var(--space-1) var(--space-4) rgba(0, 0, 0, 0.8));
        transition: transform var(--duration-300) var(--ease-out);
        will-change: transform;
        backface-visibility: hidden;
    }

    .hero-logo-container:hover .hero-logo-fixed {
        transform: scale(1.02);
    }

    /* === BOTÓN CTA - SIEMPRE BLANCO === */
    .hero-cta {
        /* Siempre blanco, sin cambio de tema */
        background: transparent;
        border: var(--border-2) solid #ffffff;
        color: #ffffff;
        
        padding: calc(var(--space-3) + var(--space-1)) calc(var(--space-6) + var(--space-2));
        font-size: var(--text-sm);
        font-weight: var(--font-semibold);
        letter-spacing: var(--tracking-wide);
        text-transform: uppercase;
        cursor: pointer;
        transition: var(--transition-base);
        text-decoration: none;
        display: inline-block;
        margin-top: var(--space-4);
        border-radius: var(--radius-md);
        
        /* Sombra siempre oscura para el texto blanco */
        text-shadow: 0 var(--space-1) var(--space-2) rgba(0, 0, 0, 0.8);
    }

    .hero-cta:hover {
        border-color: #ffffff;
        color: #ffffff;
        background: rgba(255, 255, 255, 0.1);
        transform: translateY(-2px);
        
        /* Efecto glow blanco */
        box-shadow: 
            0 0 var(--space-6) rgba(255, 255, 255, 0.8), 
            0 0 0 var(--border-1) rgba(255, 255, 255, 1);
        
        /* Text shadow con glow blanco */
        text-shadow: 0 0 var(--space-4) rgba(255, 255, 255, 0.6);
    }

    .hero-cta:active {
        transform: translateY(-1px);
    }

    .hero-cta:focus-visible {
        outline: var(--border-2) solid rgba(255, 255, 255, 0.8);
        outline-offset: var(--space-1);
    }

    /* === FOOTER DEL HERO === */
    .hero-footer {
        position: absolute;
        bottom: 0;
        text-align: center;
        z-index: 3;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: var(--gap-mobile);
        padding-bottom: calc(var(--padding-mobile) * 1.5);
        width: 100%;
    }

    /* === SCROLL INDICATOR - SIEMPRE BLANCO === */
    .scroll-indicator {
        cursor: pointer;
        transition: var(--transition-base);
        padding: var(--padding-mobile);
    }

    .scroll-indicator:hover {
        transform: translateY(calc(-1 * var(--space-1)));
    }

    .scroll-indicator:focus-visible {
        outline: var(--border-2) solid rgba(255, 255, 255, 0.8);
        outline-offset: var(--space-1);
        border-radius: var(--radius-md);
    }

    .scroll-arrow {
        font-size: var(--text-3xl);
        animation: bounce 2s infinite;
        /* Siempre blanco con sombra oscura */
        color: #ffffff;
        text-shadow: 0 var(--space-1) var(--space-2) rgba(0, 0, 0, 0.8);
    }

    @keyframes bounce {
        0%, 20%, 50%, 80%, 100% {
            transform: translateY(0);
        }
        40% {
            transform: translateY(calc(-1 * var(--space-3)));
        }
        60% {
            transform: translateY(calc(-1 * var(--space-1)));
        }
    }

    /* === RESPONSIVE USANDO TOKENS === */
    
    /* DESKTOP LARGE (1400px+) */
    @media (min-width: 1400px) {
        .hero-header {
            padding-top: calc(var(--padding-desktop) * 2.33);
            padding-left: var(--padding-desktop);
            padding-right: var(--padding-desktop);
        }

        .hero-footer {
            gap: var(--gap-desktop-lg);
            padding-bottom: var(--padding-desktop);
        }

        .hero-logo-container {
            max-width: 650px;
            margin-bottom: var(--space-12);
        }

        .hero-cta {
            padding: calc(var(--space-3) + var(--space-1)) var(--space-8);
            font-size: var(--text-sm);
            letter-spacing: var(--tracking-wide);
            margin-top: var(--space-3);
            max-width: 200px;
        }

        .scroll-arrow {
            font-size: var(--text-4xl);
        }
    }

    /* DESKTOP (1024px+) */
    @media (min-width: 1024px) and (max-width: 1399px) {
        .hero-header {
            padding-top: calc(var(--padding-desktop) * 3.5);
            padding-left: var(--padding-desktop);
            padding-right: var(--padding-desktop);
        }

        .hero-cta {
            padding: var(--space-4) var(--space-8);
            font-size: var(--text-base);
            letter-spacing: var(--tracking-normal);
            margin-top: var(--space-4);
            border-radius: var(--radius-md);
        }

        .hero-logo-container {
            max-width: 600px;
            margin-bottom: var(--space-10);
        }

        .hero-footer {
            gap: var(--gap-desktop);
            padding-bottom: var(--padding-desktop);
        }

        .scroll-arrow {
            font-size: var(--text-3xl);
        }
    }

    /* TABLET (768px+) */
    @media (min-width: 768px) and (max-width: 1023px) {
        .hero-header {
            padding-top: calc(var(--padding-tablet) * 4);
            padding-left: var(--padding-tablet);
            padding-right: var(--padding-tablet);
        }

        .hero-logo-container {
            max-width: 500px;
            margin-bottom: var(--space-8);
        }

        .hero-cta {
            padding: calc(var(--space-3) + var(--space-1)) var(--space-6);
            font-size: var(--text-base);
            letter-spacing: var(--tracking-wide);
            margin-top: var(--space-5);
        }

        .hero-footer {
            gap: var(--gap-tablet);
            padding-bottom: calc(var(--padding-tablet) * 1.5);
        }

        .scroll-arrow {
            font-size: calc(var(--text-3xl) + var(--space-2));
        }
    }

    /* MOBILE (420px+) */
    @media (min-width: 420px) and (max-width: 767px) {
        .hero-header {
            padding-top: calc(var(--padding-mobile) * 3);
        }

        .hero-logo-container {
            max-width: 420px;
            margin-bottom: var(--space-6);
        }

        .hero-cta {
            padding: var(--space-3) var(--space-5);
            font-size: var(--text-sm);
            letter-spacing: var(--tracking-normal);
        }

        .scroll-arrow {
            font-size: var(--text-2xl);
        }
    }

    /* MOBILE PEQUEÑO (0-419px) */
    @media (max-width: 419px) {
        .hero-header {
            padding-top: calc(var(--padding-mobile) * 2.5);
            padding-left: calc(var(--padding-mobile) * 0.75);
            padding-right: calc(var(--padding-mobile) * 0.75);
        }

        .hero-logo-container {
            max-width: 90%;
            margin-bottom: var(--space-4);
        }

        .hero-cta {
            padding: var(--space-2) var(--space-4);
            font-size: var(--text-xs);
            letter-spacing: var(--tracking-tight);
            margin-top: var(--space-3);
        }

        .hero-footer {
            gap: calc(var(--gap-mobile) * 0.75);
            padding-bottom: var(--padding-mobile);
        }

        .scroll-arrow {
            font-size: var(--text-xl);
        }
    }

    /* === ACCESIBILIDAD === */
    
    @media (prefers-reduced-motion: reduce) {
        .hero-logo-fixed,
        .hero-cta,
        .scroll-indicator {
            transition: none;
        }
        
        .scroll-arrow {
            animation: none;
        }
        
        .hero-logo-container:hover .hero-logo-fixed {
            transform: none;
        }
    }
    
    @media (prefers-contrast: high) {
        .hero-cta {
            border-width: var(--border-4);
            background: rgba(0, 0, 0, 0.2); /* Fondo semi-transparente para mejor contraste */
        }
        
        .hero-logo-fixed {
            filter: drop-shadow(0 var(--space-1) var(--space-4) rgba(0, 0, 0, 1));
        }
        
        .scroll-arrow {
            text-shadow: 0 var(--space-1) var(--space-2) rgba(0, 0, 0, 1);
        }
    }

    /* === MEJORAS DE RENDIMIENTO === */
    .hero-logo-container {
        contain: layout style;
    }
    
    .hero-logo-fixed {
        will-change: transform;
        backface-visibility: hidden;
    }
    
    .hero-cta,
    .scroll-indicator {
        will-change: transform;
    }

    /* === UTILIDADES DE DEBUG === */
    .hero-header.debug {
        border: 2px dashed rgba(255, 255, 255, 0.3);
    }
    
    .hero-footer.debug {
        border: 2px dashed rgba(255, 255, 255, 0.3);
    }
</style>

<!-- Script simplificado - ya no necesitamos cambio de logos por tema -->
<script>
    document.addEventListener('DOMContentLoaded', () => {
        // Funcionalidad adicional si se necesita
        // Por ejemplo, animaciones de entrada, parallax, etc.
        
        // Opcional: efecto parallax suave en el logo
        const logo = document.querySelector('.hero-logo-fixed');
        if (logo && !window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
            let ticking = false;
            
            function updateParallax() {
                const scrolled = window.pageYOffset;
                const rate = scrolled * -0.1;
                
                if (logo) {
                    logo.style.transform = `translateY(${rate}px) scale(${1 + scrolled * 0.00005})`;
                }
                
                ticking = false;
            }
            
            function requestTick() {
                if (!ticking) {
                    requestAnimationFrame(updateParallax);
                    ticking = true;
                }
            }
            
            window.addEventListener('scroll', requestTick, { passive: true });
        }
    });
</script>