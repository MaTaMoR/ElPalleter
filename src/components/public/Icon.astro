---
const {
    name,
    className = '',
    width,
    height
} = Astro.props;

const path = `/downloaded-images/${name}`;
---

<img
    class={`${className}`}
    alt={name}
    src={path}
    data-image-id={name}
    width={width}
    height={height}
/>