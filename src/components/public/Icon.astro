---
const {
    imageId,
    className = '',
    width,
    height
} = Astro.props;

const path = `/downloaded-images/${imageId}.svg`;
---

<img
    class={`${className}`}
    src={path}
    data-image-id={imageId}
    width={width}
    height={height}
/>